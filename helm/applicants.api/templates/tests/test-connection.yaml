apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "applicants.api.fullname" . }}-test-connection"
  labels:
    {{- include "applicants.api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "applicants.api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

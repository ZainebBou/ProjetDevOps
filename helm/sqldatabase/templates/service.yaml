apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqldatabase.fullname" . }}
  labels:
    {{- include "sqldatabase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqldatabase.selectorLabels" . | nindent 4 }}

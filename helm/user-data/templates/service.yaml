apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-data.fullname" . }}
  labels:
    {{- include "user-data.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-data.selectorLabels" . | nindent 4 }}

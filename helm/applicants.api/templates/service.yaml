apiVersion: v1
kind: Service
metadata:
  name: {{ include "applicants.api.fullname" . }}
  labels:
    {{- include "applicants.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "applicants.api.selectorLabels" . | nindent 4 }}

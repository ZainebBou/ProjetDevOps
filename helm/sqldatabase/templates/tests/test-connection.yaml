apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sqldatabase.fullname" . }}-test-connection"
  labels:
    {{- include "sqldatabase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sqldatabase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
